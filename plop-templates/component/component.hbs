import React from "react"
{{#if shouldAddStyles }}
import * as Style from "./{{> componentName}}.styles"
{{/if}}

function {{> componentName }}({}) {
  return null;
}

export default {{> componentName }}